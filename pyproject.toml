[project]
name = "grading-system-yjs"
version = "0.1.0"
description = "A lightweight grading system for TAs — built for consistency, speed, and fairness"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.2",
]
